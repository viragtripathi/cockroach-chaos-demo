# Docker/Podman Socket Configuration
# Copy this file to .env and uncomment the appropriate line if needed

# For Docker (default - works out of the box)
# DOCKER_SOCKET=/var/run/docker.sock

# For Podman on macOS (check if symlink exists: ls -la /var/run/docker.sock)
# If symlink exists -> no configuration needed (default works)
# If no symlink -> uncomment and update with your username:
# DOCKER_SOCKET=/Users/yourusername/.local/share/containers/podman/machine/podman.sock

# For Podman on Linux (replace 502 with your UID from `id -u`)
# DOCKER_SOCKET=/run/user/502/podman/podman.sock

# Optional: Override CockroachDB version
# CRDB_VERSION=v25.4.0
